# ZenithWheel - คู่มือการตั้งค่า Google Sheets Integration

## 📋 ภาพรวม
ระบบวงล้อสุ่มรางวัลที่สามารถซิงค์ข้อมูลกับ Google Sheets เพื่อใช้งานหลายจอพร้อมกัน รองรับ CRUD (Create, Read, Update, Delete) แบบเรียลไทม์

## 🚀 ขั้นตอนการตั้งค่า Google Sheets

### ขั้นที่ 1: สร้าง Google Spreadsheet
1. ไปที่ [Google Sheets](https://sheets.google.com)
2. สร้าง Spreadsheet ใหม่
3. ตั้งชื่อว่า "ZenithWheel Data" (หรือชื่ที่คุณต้องการ)

### ขั้นที่ 2: เปิด Google Apps Script
1. ใน Google Sheets ที่สร้างไว้ คลิกที่เมนู **Extensions** > **Apps Script**
2. จะเปิดหน้าต่าง Apps Script Editor ขึ้นมา
3. ลบโค้ดเริ่มต้นทิ้ง

### ขั้นที่ 3: วางโค้ด Apps Script
1. คัดลอกโค้ดทั้งหมดจากไฟล์ `Code.gs`
2. วางลงใน Apps Script Editor
3. คลิก **Save** (💾) และตั้งชื่อโปรเจค เช่น "ZenithWheel API"

### ขั้นที่ 4: Deploy Web App
1. คลิกที่ **Deploy** > **New deployment**
2. คลิกที่ไอคอน⚙️ (เฟือง) ข้างๆ "Select type"
3. เลือก **Web app**
4. ตั้งค่าดังนี้:
   - **Description**: ZenithWheel API v1
   - **Execute as**: Me (อีเมลของคุณ)
   - **Who has access**: Anyone (ใครก็เข้าถึงได้)
5. คลิก **Deploy**
6. อนุญาตการเข้าถึง:
   - คลิก **Authorize access**
   - เลือกบัญชี Google ของคุณ
   - คลิก **Advanced** (ถ้ามีคำเตือน)
   - คลิก **Go to [ชื่อโปรเจค] (unsafe)**
   - คลิก **Allow**
7. **คัดลอก Web app URL** ที่ได้ (จะมีลักษณะประมาณนี้):
   ```
   https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxx/exec
   ```

### ขั้นที่ 5: ตั้งค่า Web App
1. เปิดไฟล์ `googleSheets.js` ในโปรเจคของคุณ
2. แก้ไขบรรทัดนี้:
   ```javascript
   webAppUrl: 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE',
   ```
   เป็น:
   ```javascript
   webAppUrl: 'https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxx/exec',
   ```
   (ใช้ URL ที่คัดลอกไว้จากขั้นตอนก่อนหน้า)
3. **บันทึกไฟล์**

### ขั้นที่ 6: Deploy เว็บไซต์
คุณสามารถ Deploy เว็บไซต์ได้หลายวิธี:

#### วิธีที่ 1: GitHub Pages (แนะนำ)
1. สร้าง Repository ใหม่บน GitHub
2. อัพโหลดไฟล์ทั้งหมด (index.html, style.css, script.js, googleSheets.js, Logo.svg, BGblur.svg)
3. ไปที่ Settings > Pages
4. เลือก Source: main branch
5. คลิก Save
6. เว็บไซต์จะพร้อมใช้งานที่: `https://[username].github.io/[repository-name]/`

#### วิธีที่ 2: Netlify
1. ไปที่ [Netlify](https://www.netlify.com/)
2. ลาก-วางโฟลเดอร์โปรเจคทั้งหมด
3. เว็บไซต์จะ Deploy อัตโนมัติ

#### วิธีที่ 3: Vercel
1. ไปที่ [Vercel](https://vercel.com/)
2. Import โปรเจคจาก GitHub
3. Deploy อัตโนมัติ

## 📱 การใช้งาน

### เปิดใช้งาน Google Sheets
เมื่อตั้งค่า Web App URL เรียบร้อยแล้ว ระบบจะ:
- ✅ โหลดข้อมูลรางวัลจาก Google Sheets อัตโนมัติ
- ✅ บันทึกการเพิ่ม/แก้ไข/ลบรางวัลไปยัง Google Sheets
- ✅ อัพเดทจำนวนรางวัลเมื่อหมุนวงล้อ
- ✅ บันทึกประวัติการหมุนใน Sheet "WinHistory"

### การใช้งานหลายจอ
1. เปิดเว็บไซต์ในหลายเบราว์เซอร์/อุปกรณ์
2. ข้อมูลจะซิงค์กันอัตโนมัติผ่าน Google Sheets
3. เมื่อหมุนวงล้อในจอใดจอหนึ่ง ให้รีเฟรชหน้าเว็บในจออื่นๆ เพื่อดูข้อมูลที่อัพเดท

### ดูข้อมูลใน Google Sheets
ระบบจะสร้าง 2 Sheets อัตโนมัติ:

#### 1. **Prizes Sheet**
เก็บข้อมูลรางวัลปัจจุบัน:
- ID: รหัสรางวัล
- Name: ชื่อรางวัล
- Color: สีของรางวัล
- Quantity: จำนวนคงเหลือ
- Original Quantity: จำนวนเริ่มต้น
- Last Updated: เวลาที่อัพเดทล่าสุด

#### 2. **WinHistory Sheet**
บันทึกประวัติการหมุนทั้งหมด:
- Timestamp: วันเวลาที่หมุน
- Prize ID: รหัสรางวัลที่ได้
- Prize Name: ชื่อรางวัลที่ได้
- Prize Color: สีของรางวัล
- Remaining Quantity: จำนวนคงเหลือหลังหมุน

## 🔧 ฟีเจอร์ CRUD

### Create (เพิ่มรางวัล)
- เพิ่มรางวัลผ่านหน้า "จัดการรางวัล"
- ข้อมูลจะถูกบันทึกไปยัง Google Sheets ทันที

### Read (อ่านข้อมูล)
- โหลดข้อมูลจาก Google Sheets เมื่อเปิดหน้าเว็บ
- แสดงรายการรางวัลทั้งหมด

### Update (แก้ไขรางวัล)
- เพิ่ม/ลดจำนวนรางวัลด้วยปุ่ม +/−
- อัพเดทข้อมูลไปยัง Google Sheets ทันที
- อัพเดทจำนวนเมื่อหมุนวงล้อได้รางวัล

### Delete (ลบรางวัล)
- ลบรางวัลด้วยปุ่ม "ลบ"
- ข้อมูลจะถูกลบจาก Google Sheets ทันที

## ⚙️ การทดสอบ

### ทดสอบการเชื่อมต่อ
1. เปิด Console ของเบราว์เซอร์ (F12)
2. ดูข้อความ:
   - `Google Sheets integration enabled` = เชื่อมต่อสำเร็จ
   - `Using LocalStorage (Google Sheets not configured)` = ยังไม่ได้ตั้งค่า URL

### ทดสอบฟังก์ชัน CRUD
ใน Apps Script Editor:
1. คลิกที่ฟังก์ชัน `testRead()` หรือ `testCreate()`
2. คลิก **Run**
3. ดูผลลัพธ์ใน **Execution log**

## 🌐 การใช้งานกับ Live Server (localhost)

Google Sheets Integration **ใช้งานได้ทั้ง Deploy และ Live Server** โดยไม่ต้องแก้ไขอะไรเพิ่มเติม!

ไฟล์ `Code.gs` ที่ให้ไปแล้วมี CORS headers ในตัว รองรับการใช้งานทั้ง:
- ✅ Live Server (http://localhost หรือ http://127.0.0.1)
- ✅ Deploy (GitHub Pages, Netlify, Vercel)
- ✅ ใช้ Web App URL เดียวกันได้เลย ไม่ต้องเปลี่ยน

### วิธีใช้กับ Live Server:
1. เปิด VS Code
2. คลิกขวาที่ `index.html`
3. เลือก "Open with Live Server"
4. เว็บจะทำงานพร้อม Google Sheets ทันที!

### วิธีใช้กับ Deploy:
1. Push โปรเจคไป GitHub
2. เปิด GitHub Pages
3. เว็บจะทำงานพร้อม Google Sheets ทันที!

**ข้อสำคัญ:** หลังจาก Deploy Apps Script แล้ว ถ้าคุณแก้ไขโค้ดใน `Code.gs` ต้อง Deploy ใหม่อีกครั้ง:
1. ไปที่ Apps Script Editor
2. คลิก **Deploy** > **Manage deployments**
3. คลิกไอคอน ✏️ (แก้ไข)
4. เลือก **New version**
5. คลิก **Deploy**

## 🐛 การแก้ปัญหา

### ปัญหา: ไม่สามารถโหลดข้อมูลจาก Google Sheets
**แก้ไข:**
- ตรวจสอบว่า Web App URL ถูกต้อง
- ตรวจสอบว่าตั้งค่า "Who has access" เป็น "Anyone"
- ลอง Deploy ใหม่อีกครั้ง

### ปัญหา: CORS Error (เมื่อใช้ Live Server)
**แก้ไข:**
- เพิ่ม CORS headers ใน Code.gs (ตามขั้นตอนด้านบน)
- Deploy Apps Script ใหม่หลังจากแก้ไข
- ถ้าใช้ Deploy แบบ HTTPS จะไม่มีปัญหานี้

### ปัญหา: CORS Error (เมื่อ Deploy แล้ว)
**แก้ไข:**
- ใช้ HTTPS ในการ Deploy เว็บไซต์ (GitHub Pages, Netlify, Vercel รองรับอัตโนมัติ)
- ตรวจสอบว่า Web App ถูก Deploy แล้ว

### ปัญหา: ข้อมูลไม่ซิงค์ระหว่างจอ
**แก้ไข:**
- รีเฟรชหน้าเว็บในแต่ละจอ
- ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
- ดูใน Console ว่ามี Error หรือไม่

## 📊 โครงสร้างไฟล์

```
ZenithWheel/
├── index.html          # หน้าเว็บหลัก
├── style.css           # ไฟล์ CSS
├── script.js           # ไฟล์ JavaScript หลัก
├── googleSheets.js     # ไฟล์สำหรับเชื่อมต่อ Google Sheets
├── Code.gs             # Google Apps Script (วางใน Apps Script Editor)
├── Logo.svg            # โลโก้
├── BGblur.svg          # พื้นหลัง
└── README.md           # คู่มือนี้
```

## 🔐 ความปลอดภัย

- ข้อมูลถูกเก็บใน Google Sheets ของคุณเอง
- สามารถตั้งค่าสิทธิ์การเข้าถึง Spreadsheet ได้
- Web App URL เป็นข้อมูลสาธารณะ แต่ทำงานเฉพาะกับ Spreadsheet ที่กำหนด

## 📞 ติดต่อ & สนับสนุน

หากมีปัญหาหรือคำถาม:
1. ตรวจสอบ Console log ของเบราว์เซอร์
2. ตรวจสอบ Execution log ใน Apps Script
3. ตรวจสอบว่าทุกขั้นตอนถูกต้องตามคู่มือนี้

## 🎉 เสร็จสิ้น!

ตอนนี้คุณสามารถใช้งาน ZenithWheel กับ Google Sheets แล้ว ข้อมูลจะซิงค์กันทุกครั้งที่มีการเปลี่ยนแปลง และสามารถใช้งานหลายจอพร้อมกันได้!
